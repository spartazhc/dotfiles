
- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.p10k.zsh: p10k.zsh
    ~/.zsh:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
